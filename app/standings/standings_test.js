'use strict';

describe('tracker.standings module', function() {

  beforeEach(module('tracker.standings'));

  describe('standings controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var standingsCtrl = $controller('StandingsCtrl');
      expect(standingsCtrl).toBeDefined();
    }));

  });
});